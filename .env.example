# Rakta Production Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# ===========================================
# RENDER DEPLOYMENT (render.yaml handles most of these)
# ===========================================
# When deploying on Render:
# - DATABASE_URL: Auto-provided by Render Postgres (parsed by docker-entrypoint.sh)
# - PORT: Auto-injected by Render for each service
# - JWT_SECRET: Set manually in Render dashboard (sync: false)
# - GOOGLE_CLIENT_ID/SECRET: Optional, set in dashboard if using OAuth
# - NEXT_PUBLIC_API_URL: Set in render.yaml, update after custom domain setup

# ===========================================
# Domain Configuration (REQUIRED)
# ===========================================
DOMAIN=sungchunn.com
LE_EMAIL=your-email@example.com

# ===========================================
# PostgreSQL Database (REQUIRED)
# ===========================================
POSTGRES_DB=rakta
POSTGRES_USER=rakta_user
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# ===========================================
# JWT Configuration (REQUIRED)
# ===========================================
# Generate a secure secret with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET_AT_LEAST_64_CHARACTERS_USE_OPENSSL_RAND_BASE64_64

# ===========================================
# Email Configuration (Optional)
# ===========================================
# Uncomment and configure if you need email functionality
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# ===========================================
# OAuth2 Configuration (Optional but Required for Startup)
# ===========================================
# If not using Google OAuth, use placeholder values
GOOGLE_CLIENT_ID=placeholder
GOOGLE_CLIENT_SECRET=placeholder

# ===========================================
# Production Notes
# ===========================================
# 1. Always use strong, unique passwords for POSTGRES_PASSWORD
# 2. JWT_SECRET should be at least 64 characters for security
# 3. LE_EMAIL is used by Let's Encrypt for renewal notifications
# 4. Ensure DNS records point to your VPS IP:
#    - A record: sungchunn.com -> your_vps_ip
#    - A record: www.sungchunn.com -> your_vps_ip
#    - A record: api.sungchunn.com -> your_vps_ip
# 5. Ports 80 and 443 must be open on your VPS firewall
