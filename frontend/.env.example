# FitSloth Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control

# ===========================================
# Backend API Configuration (REQUIRED)
# ===========================================
# Base URL of the backend API
# For local development, use: http://localhost:8080
# For production, use your deployed backend URL (e.g., https://api.fitsloth.com)
NEXT_PUBLIC_API_URL=http://localhost:8080

# ===========================================
# Next.js Configuration
# ===========================================
# Port for the development server (optional, defaults to 3000)
# PORT=3000

# Node environment (usually set automatically)
# NODE_ENV=development

# ===========================================
# Analytics & Monitoring (Optional)
# ===========================================
# Add analytics IDs when ready to track usage

# Google Analytics measurement ID
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ===========================================
# Feature Flags (Optional)
# ===========================================
# Enable/disable features during development

# Enable debug mode
# NEXT_PUBLIC_DEBUG_MODE=false

# Enable experimental features
# NEXT_PUBLIC_ENABLE_EXPERIMENTAL=false

# ===========================================
# Production Settings
# ===========================================
# For production deployments:
# - Set NEXT_PUBLIC_API_URL to your production backend URL
# - Ensure the backend URL uses HTTPS
# - Remove or disable debug/development flags
# - Configure proper CORS settings on the backend
# - Set up proper error tracking (Sentry, etc.)

# ===========================================
# Important Notes
# ===========================================
# 1. All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# 2. Never put sensitive keys or secrets in NEXT_PUBLIC_ variables
# 3. Restart the dev server after changing environment variables
# 4. For Vercel deployment, set these variables in the Vercel dashboard
# 5. For local development, copy this file to .env.local
