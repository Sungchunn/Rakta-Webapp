import MetricInputForm from "@/components/MetricInputForm";

export default function LogPage() {
    return (
        <div>
            <h1 style={{ fontSize: "1.5rem", marginBottom: "1rem" }}>Daily Log</h1>
            <MetricInputForm />
        </div>
    );
}
